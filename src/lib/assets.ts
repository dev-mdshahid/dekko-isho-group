export const legacyImage = (path: string) => `/legacy/images/${path}`

export const legacyVideo = (path: string) => `/legacy/videos/${path}`

export const legacyDoc = (path: string) => `/legacy/documents/${path}`
